## [2026-06-14] session-end | PTHub | Major UI iteration + persistence fix. Sidebar removed, dashboard redesigned, per-section unit tabs, milestone detail fields, data baked into HTML. MFP CSV import requested � UNVERIFIED at close.
## [2026-06-15] session-end | PTHub | Cardio builder, RTL time input, event types, sort bar simplified to name-only A?Z/Z?A
## [2026-06-19] session-end | PTHub | Log Session modal built (overlay, 1RM auto-fill, RPE/RIR toggle, set rows); template builder UX pass (AMRAP/Uni/Timed to top, RPE/RIR two-button toggle, %1RM?weight auto-fill, rest format-on-blur); Python preview server. Interrupted: Notes field + muscle group dropdown pending.
2026-06-19 | CoachApp session 2 | PTHub frozen as spec; CoachApp set form rebuilt to match PTHub layout; rest MM:SS parsing fixed; muscle groups multi-select + notes textarea added to PTHub; session protocol established (hello claude trigger reads Vault); migration order agreed: weight tracking ? performance/PBs ? calendar ? programs
2026-06-19 | CoachApp session 3 | Product blueprint locked: three account types (coach/client/solo), invite flow agreed, calendar model agreed, permissions model agreed; Supabase schema updated (client role, coach_id nullable, events table, one_rm_kg, muscle_groups); weight tracking UI not yet started � next session
2026-06-19 | CoachApp session 4 checkpoint | Weight tracking UI + Chart.js chart built; clients.user_id added; full client RLS policies across all tables confirmed in Supabase
2026-06-19 | CoachApp session 4 full | Weight chart (Chart.js), performance tab (4 categories), invite system end-to-end (Edge Function, set-password form, Netlify deploy); known bug: user_id stamping not working after invite acceptance
2026-06-20 | CoachApp session 5 | Invite system fully fixed: Edge Function stamps user_id at send time using inviteUserByEmail return value + service role. Redundant triggers/RPCs dropped. handle_new_user rewritten to doc-correct pattern. clients.invited_at added. Client header: Send/Resend invite + Update email + Update details. Going-in-circles audit protocol added to session start. v=15 deployed.
2026-06-20 | CoachApp session 6 | git init + initial commit; run-coachapp skill built and verified live; hello-claude global skill created (auto-load UNVERIFIED — test next session); /save confirmed as built-in; three client header buttons added (v=15, not yet deployed); slash command reference banked.
2026-06-20 | CoachApp session 7 | Compliance card, test client cleanup, 11 client RLS policies, broken auth.users policy fixed, header buttons verified, silent failure audit, sql-safety skill created. v=22 committed, pending Netlify deploy.
2026-06-21 | CoachApp | Workout Runner built (real-time gym logger, one set at a time, LOG button, live stats, save to DB); client Workouts page with template list + ▶ Start; master account view switcher (PT|Client pill, auto-detects dual role); mobile-check skill + feedback memory created; v=34 live on Netlify

2026-06-22 | CoachApp | Bug audit (7/10 fixed), pre-push hook, hello-claude auto code review, role routing fix, repo cleanup explained | v=52 on GitHub Pages
2026-06-22 | CoachApp | v63-v65 | Cardio runner Done-early fix, session history PGRST200 fix, template card full set preview, runner coach notes, dbq() error wrapper, auth re-render guard | 3 commits pushed to GitHub Pages | UNVERIFIED: openWorkoutLog end-to-end, runner coach note display | TODO: Jake to run FK constraint SQL in Supabase
2026-06-23 | CoachApp v76 | CI pipeline (GitHub Actions), checks.sh tracked, RLS optimised (SELECT auth.uid() wrapper, 10 duplicates dropped, missing client INSERT policy on workout_log_sets added), progress photo transforms. PAT workflow scope needed before CI goes live.
2026-06-23 | CoachApp memory expansion | user_jake profile, dual-explanation rule, post-build-review, skill-creator, verify, code-review habits all banked as memory entries. 16 entries in MEMORY.md. No code changes.
2026-06-24 | CoachApp | v79-v80 | dashboard grid responsive; E2E client test; post-save navigation bug fixed; client session history tappable; E2E testing lessons banked in memory
2026-06-24 | CoachApp | Playwright E2E suite (14/14), seed script, skills overhaul (/save /playwright /deploy-check), hello-claude rewrite, /code-review wired in, daily question cron, voice deltas banked | v86-v93 pushed

2026-06-24 | CoachApp | hello-claude + /save audit | Root cause of stale to-dos fixed. Both hello-claude files (global + project) fully synced to 8-step ritual with mandatory reads, to-do verification, and predictions review. /save sweep-before-add pattern added.

2026-06-25 | CoachApp | Programs v1 build (v94) + modal layout bug fix (v95) | Pushed to GitHub Pages | Next: full Programs E2E + UX review
2026-06-25 | CoachApp | v97-v98 | Hyrox Week 1 built (6 templates, assigned Mon-Sat Base Building phase); cardio template display overhauled (MM:SS, section labels, paceKm/restHr/strokeRate); runner session brief + target chips; expandable client workout cards | pushed and deployed
2026-06-25 | CoachApp | v110 | Role-specific nav, compact runner input, last session strip, My Progress page (4 tabs), template edit/delete guard, runner chart removed | pushed to GH Pages | open: My Progress live test (RLS + PostgREST join), roadmap update

2026-06-26 | CoachApp | 12-week Hyrox program built (44 Option B templates, W2-W12), client_1rms table, client calendar tab + day detail modal, PT template filter, dashboard strip removed | v120-v121 | Discuss-then-approve working agreement established
2026-06-27 | CoachApp | Runner feature pack (timed sets, unilateral, My Programs accordion, runner redesign, skip rest fix, session detail fix), Playwright hardened (15/15 green) | v134 local � push pending | jake
2026-06-28 | CoachApp | v135→v143 | UI consistency pass (client day modal, PT Programs accordion, client Workouts page — all surfaces aligned); edit start date feature; normalizeDuration cardio fix; Test Client created; E2E smoke test 9/9 PASS | Open: verify calendar on live, Test Client auth invite
2026-06-28b | Vault | — | pthub repo renamed to vault; Vault remote wired + first push; /save skill updated to auto-commit vault at session end | No open items
2026-07-02 | CoachApp session 8 pt5 | Four "workaround-every-session" root-cause fixes: preview parent-template seed path (4th occ), /wiki-ingest skill + wiki/sources.md manifest (ends re-reading), /save->/vault-save golden-path rename committed f6f3c48 + hello-claude Step 9 self-check. LLM wiki batch-3 ingest: 8 re-clips recycled, 32 new sources, new coachapp-spreadsheet-competition page. NO CoachApp code — cache-bust/Playwright N/A.
2026-07-02 | CoachApp session 9 | Runner v1 redesign shipped: Hevy-style all-sets-visible strength table (SET/PREVIOUS/KG/REPS/checkmark), non-blocking rest timer, 1-tap repeat from last session — plain strength only, cardio/timed/unilateral/percent-1RM stay on wizard (phase 2). Tap-target bug (32px to 44px) caught+fixed pre-ship. 48/48 full Playwright + 12/12 runner spec (2 new smoke tests) + 3-agent review (security/scoping, solo-mode, duplicates+PII, all SHIP) before push. v=6 pushed 6e6402a, CI green, Pages deployed. Also: fixed dead PT-Dashboard launch config auto-starting over CoachApp (worktree + seed template, not all pre-existing worktrees). Open: superset auto-advance not replicated in table (check if real templates use it), bodyweight-in-table not live-verified, PAT exposed in git remote URL.
2026-07-03 | CoachApp session 10 | 13 fixes from Jake live test run (create program->generate 4wk->run it): strength-table target-info bar restored + inline 0:00 rest timer + visible tick button; mid-workout swap/add exercise (session-only); voice-cue fix (real gesture-tied speak, cancel() alone never primed) + female voice; create-new-workout auto-assigns to its day slot; Edit button on session-detail drawer; phase card shows periodization range; tempo 4-char cap. programs v6/workouts v8/runner v7. Playwright 51 passed + 3-agent review + 2 new smoke tests. Fixed picker-modal-behind-runner z-index bug (caught by tests). Cleaned 65 orphaned duplicate templates from Jake account via FK-enum->reference-check->self-guarded transaction delete. Authored wiki pages coachapp-programs-architecture + coachapp-runner-architecture. deleteProgram() orphan bug CONFIRMED LIVE (the deleted W2/W3/W4 clones were its debris). Pushed 5438aac.
2026-07-03 | CoachApp session 11 | Runner set-accuracy build (per-set targets fixed, delete-a-set, live rep tally, 1RM-aware picker) + swap/add unified to literal builder modal after Jake live-tested and corrected tick/delete/correlation design | feature-audit skill created | deleteProgram() orphan root cause reconfirmed via full FK enumeration, read-only diagnostic SQL handed to Jake | NOT YET PUSHED — app-runner v8, app-workouts v9, Playwright 56/56

2026-07-03 | CoachApp session 12 | Runner build pushed (1914e7b) after fresh 3-agent review + dead code removed | 2 scoping bugs found+fixed: showLogSessionModal unscoped query, deleteProgram missing coach_id (dee1479, runner v9/programs v7) | deleteProgram() full rewrite: blocks on assigned clients, cleans up owned templates -- researched live FK cascade rules first, verified against real Test 1 program + disposable fixture (66bf1fd, programs v8) | GitHub Pages dual-workflow bug found+fixed: switched Pages source to Actions-only after Jake approval | Playwright 56/56 | Open: watch next pushes for Pages fix durability, 993-template historical backlog cleanup still pending

2026-07-03 | CoachApp session 13 | Architecture/infra audit (5 areas, parallel agents): removed dead graphify (hooks/graph/CLAUDE.md, coachapp 609d5ee); fixed 3 stale skill paths (app.js->8 modules) + mobile-check 390->480 + test counts 47/14->real; bounded hello-claude session-start reads; new pinned multi-agent-review skill + weekly full-file mode; wiki+roadmap refreshed (Vault f3706a1). CONT: Vault=one repo w/ framework (finding); coachapp PAT leaked via git remote get-url -> Jake deleted, remote URL cleaned, gh auth setup-git, verified empty push ec30ebf 27/27; banked accurate-steps feedback + les-018 (do not echo known secrets). No module versions changed.

2026-07-03 | CoachApp session 13 (cont 2) | ~/.claude had no offsite backup (skills=OS + auto-memory, local-disk-only) while app+Vault both on GitHub. Created private repo jakendwest-ops/claude-config, git init in ~/.claude w/ allowlist .gitignore (tracks only skills/ + memory/, excludes settings.json/sessions/caches), secret-scanned before push, tokenless gh auth. Initial 4f7d324 (55 files) + wired /save Step 5 to keep current + reference_claude_config_backup.md (55dba6c). Wiki left as-is (OneDrive-synced). No app code.
2026-07-04 | CoachApp session 14 | Duplicate week (unified renderPhaseWeekGrid, every week equally editable, programs v9) + fork-on-edit for shared workout templates (_resolveEditableTemplateId clones before mutating a template shared across phase slots, workouts v10) + deleteProgram() fix (solo self-assignment no longer blocks deletion, PT toast names blocking clients) | 3-agent review caught+fixed a stale confirm-dialog wording gap (Generate weeks now warns about manual week content too, not just periodization output) | 3 new Playwright tests, 59/59 green, live-verified against real Supabase data before push (730738a), CI green | banked les-020 (git-push-timeout/screenshot-retry/network-dump efficiency lesson) + pth-085 after Jake asked why the session ran 300k tokens/1hr+
2026-07-04 | CoachApp session 15 | Runner exercise-picker freeze fix (hardcoded modal id + fetch-latency race let a fast Swap-then-Add tap spawn two overlapping modals, unrecoverable without reload -- found live during Jake's own gym session, lost his workout data) + beep-to-speech fix for the 5-4-3-2-1 rest-timer countdown (iOS suspects to suspend Web Audio mid-rest; Web Speech doesn't) -- 3-agent review caught+fixed a real bug in the fix itself (cardio timer never primed speech synthesis), new Playwright regression test, pushed 84f9267 | Quick wins: scoped 5 unguarded clients queries by coach_id in app-clients.js + tightened a flaky Playwright locator, pushed f997474 (both pre-push-hook + CI green after a transient GitHub Pages deploy retry) | Corrected a stale roadmap.md fact (solo coach_id is NULL not auth.uid()) | New standing rule: roadmap.md additions now mirror into the LLM wiki same-turn (feedback_roadmap_wiki_sync.md) | Open: session-autosave design decision + workout-tracking-visuals scoping, both awaiting Jake
2026-07-05 | CoachApp session 16 | Dashboard CSS consistency pass shipped (main.css v4, app-dashboard v2, 313bc74): fixed undefined .dashboard-card CSS (37x, no bg/border/shadow across all 3 dashboards), consolidated 3 duplicated grid style blocks, 4 bare .btn Cancel buttons, hardcoded hex to design tokens, PT/solo mobile stat-strip bugs. 3-agent review clean, 2 new Playwright tests, CI green. Runner autosave scoped + designed (hybrid localStorage draft) via Plan agent but not built -- session interrupted, ready to build next session. Found (not fixed): app-wide undefined CSS vars (52x/7 files), .modal-box undefined, app-programs.js:672 same btn bug, and a claude-config backup gap (per-project auto-memory dirs not allowlisted). 5 predictions graded correct; 1 new prediction banked.
2026-07-05 | CoachApp session 17 | Jake live-tested a real gym session + full app end-to-end, reported 16 bugs/features in one pass -- organized into 4 areas (Runner/Progress-Stats/Personal-Solo/Dashboard) in roadmap.md's new Session backlog section, mirrored to wiki same-turn | Built + tested Areas 1/2/4: rest-time-not-overwritten on swap/add, delete-button spacing, redundant RPE label, save-workout false-positive-error (dbq showUserError default, found+fixed in 3 places + a real stuck-button bug) -- Area 1; cardio-blank-preview fix (reused existing template-card formatter), bodyweight Y-axis 0.5kg steps + new weight-goals feature (2 new clients columns + new client-role RLS UPDATE policy), update-1RM-modal-outside-modal fix (.modal-box undefined CSS, 5 sites) + bonus z-index fix on mid-workout 1RM sheet -- Area 2; dashboard Current-program header + View-program button -- Area 4 | Live bug hunt with Jake mid-session: Trap Bar Jump UI inconsistency traced to %1RM (intensityMin) exclusion from the strength table, not name-matching as originally theorized -- scoped to '%1RM only' per Jake, one-line routing fix since the table's target bar already computed %1RM display | app-dashboard v2->3, app-workouts v11->12, app-runner v10->13, app-progress v3->4, 8 files +234/-27 lines, NOT YET PUSHED (needs multi-agent review first) | Full suite 69/69 green throughout | 2 skills updated (hello-claude all-exercise-types check, feature-audit destructive-spacing check) | New gap surfaced+banked: clients can self-detach from PT via row-level RLS | les-023 (dotenv false-alarm) + les-024 (let-scoped var across script tags) banked | 4 new predictions (pth-089 to pth-092)
2026-07-07 | CoachApp session 20 | Fixed slow workout save (batched saveRunnerSession/saveWorkoutSession N+1 inserts into 2 batched inserts each, correlated by order_index; added rollback to saveWorkoutSession, which never had one; measured 14 req/4.7s -> 4 req/1.1s on a 6-exercise save) + slow Workouts-page load (.limit(100) on both template queries) -- pushed 444d0f3 | Found+fixed a real stray-character syntax corruption in app-runner.js mid-session (traced via live browser console capture, not guessed) | Live diagnostic resolved a stale estimate: orphaned workout_templates backlog was 103 rows, not ~993 as previously guessed -- 0 in use, Jake ran cleanup, confirmed 0 remaining | Handed Jake a broader exercises-library cleanup SQL (delete all except personal-account-linked, explicitly accepting collateral impact on real clients' template/log links) -- outcome not yet confirmed back | Jake reported + fixed live: exercise picker modal shrinking/drifting toward the bottom of the screen as search results narrow (max-height with no fixed height + mobile bottom-anchored overlay) -- fixed with height:70vh, pushed 682f86f | 3-agent review caught 1 real gap (new rollback test's cleanup depended on the rollback it was testing) -- fixed | Fixed a stale path in the save skill itself (wrong project memory dir) | 69/69 Playwright throughout | 3 new lessons banked (les-026 stray-char/BOM detection, les-027 flushLogState DOM-read-back test gotcha, les-028 SQL temp-table over repeated CTEs)
2026-07-08 | CoachApp session 21 | Fixed solo-runner broken-screen bug (_afterRunnerSave missing solo branch, found via code review) + exercise-picker mobile-keyboard shrink (visualViewport sync, root-caused via targeted question not a second guess) | Confirmed exercises-library cleanup (51 remaining) | Session ran under tight usage budget (9% flagged) -- Jake made explicit review-depth and stop-point tradeoffs given it | app-runner v16 (298d88d), app-workouts v16 (b1aa50c), both pushed + CI green
2026-07-08 | CoachApp session 22 | Performance/Personal Bests restructure shipped (e600010): Cardio+1RMs folded into Personal Bests, Performance split into Per-session/Per-exercise tabs -- 3-agent review caught+fixed a stale-cache race between Client/Personal view switches and a Chart.js leak on every search keystroke | 3 bugs fixed (6d8c6a8): dead Log PB button, Body Weight Starting-tile+Y-axis bug, hardened workout-template coach_id check | Session not /saved at the time -- backfilled into STATUS/LOG the following session
2026-07-10 | CoachApp session 23 | Finished the Workouts-polish build (hero card + Recent-sessions rename) interrupted the prior session -- fixing its test flakiness surfaced a critical data leak (missing generated_from_phase_id filter, 2 sites), a real deleteProgram() data-loss bug (deleted templates it didn't own; 3-agent review caught a regression in the first fix), a Workouts-page perf issue (unconditional unused query), and a live empty-phase crash (pre-existing accordion code, hotfixed same-day) | Found+fully resolved a critical multi-table RLS gap: client_programs plus 3 embedded tables (programs/program_phases/program_phase_workouts) all lacked client-read policies -- real clients couldn't see their assigned program at all; first verification pass only checked the outer table, caught and corrected after Jake asked to remember it | New standing skill missed-check-to-test created (converts 'checked A, not related B' misses into a same-commit Playwright test) | Full Playwright suite green (91/91 + 2 expected skips, zero test.fixme remaining) throughout | les-031/les-032 + pth-096 to pth-098 banked
2026-07-10 | CoachApp session 24 | RLS audit scoped for 2 known gaps found a 3rd, more severe one one level deeper: workout_template_exercises had ZERO client-read policy at all -- same solo-shared-auth.uid() blind spot as session 23's client_programs bug -- plus client_1rms missing write policies for a real coached client; both fixed and verified end-to-end against a real client-role account | Built 4 features in one session: runner session autosave (localStorage draft, wired into launchRunner() -- the true single choke point, a refinement from the original startWorkoutRunner() scoping note), %1RM target rounding (floors to 2.5kg, one shared function fixes every display site), Delete week button (reuses deleteProgram's ownership-aware template-delete check), plate calculator (deliberately placed in the existing target bar, not new under-row text Jake had already rejected in that table) | Found+fixed a live bug via Jake's own screenshot mid-session: program picker showing every workout ever created for a program with indistinguishable duplicates -- root cause was a missing .is('program_id', null) filter, fixed same session, inline option relabeled "(this day only)" per Jake's explicit follow-up | Exercises table PT/Personal scoping (new is_personal column) -- found live, fixed same session, existing data left merged per Jake's explicit "fix forward, don't reclassify history" call (new memory) | First full 3-agent multi-agent-review run against a 7-commit/8-file diff caught 3 real issues before push, all fixed: deletePhaseWeek could destroy a template a sibling week's un-forked-duplicate row still needed, resumed drafts skipped the audio/speech-unlock gesture, runner drafts were never cleared on sign-out (PII on shared devices) | E2E test debris cleanup: 73 programs + 45 templates deleted via the real deleteProgram() function, not raw SQL | 9 commits pushed (7ae59d2..9f33da0), CI green, full Playwright suite green throughout (116 run, 114 passed, 2 expected skips) | les-033/les-034 banked, feedback_rls_embed_chains.md reinforced, new memory feedback_fix_forward_not_retroactive.md | app-core v2, app-programs v14, app-workouts v23, app-runner v20

## 2026-07-11 — CoachApp session 25

Shipped the Personal/solo **Library** page (Templates + Exercise Library) — solo previously had no route to the template builder at all, so reusable workouts could only be created inline from a program phase slot, locked to one day. Required a new workout_templates.is_personal column (PT and solo share one coach_id/auth.uid(), so the builder would otherwise bleed personal templates into the real client-facing library). Also fixed a pre-existing instance of the same bleed in openProgram's day-slot picker.

Auditing that split surfaced a **real cross-client RLS leak nobody knew about**: the `Client reads workout templates` policy scoped by coach_id alone, with no client_id restriction — so any client could read any OTHER client's personalised template clones. Reproduced live as a genuine client before fixing (red/green). Closed with `client_id is null`; also fixed a scalar `=` subquery in the same policy that errors for any user with >1 clients row (the master account has two).

Key judgment call: **deliberately did NOT enforce is_personal at RLS**, despite the finding being phrased as a security gap. Tracing the embed chains first showed it would have silently nulled the client dashboard/calendar master-template embed (the sessions-23/24 PostgREST failure mode). is_personal answers 'which library is this in', not 'who may read this'.

2 commits (0a3ef1d, c4b1e67), CI green, 119 Playwright passing. app-core v3 / app-programs v15 / app-workouts v24.

## 2026-07-11 — CoachApp session 25 (part 2)

Built the **Library bridge** — Jake's three asks turned out to be one problem. He'd built his personal program entirely with `+ Create new workout (this day only)`, which stamps program_id, and session 24 deliberately excluded those from the reuse pool — leaving NO bridge from 'built it in a program' to 'reuse it'. Shipped: copy program workouts to Library (per-workout + bulk, idempotent), a tap-row workout picker replacing the native `<select>` (an `<option>` can only hold plain text, which is exactly why three 'Upper Body' workouts were indistinguishable), and Duplicate-week auto-extend.

The multi-agent review on that work found a **CRITICAL data-loss bug already live**: Duplicate week → Generate weeks DESTROYED the Week-1 workout. `_cleanupPhaseWeeksBeyond` deleted every template a stale week referenced with no ownership and no still-referenced check — while its sibling `deletePhaseWeek` had both, added the day before. Duplicated weeks share the source's template_id by design, so cleanup harvested Week 1's own template and deleted it. Proved red/green (revert the fix → survived=0). Both now share `_deleteOwnedUnreferencedTemplates`. 6 more review findings fixed.

Also: repaired `seed-test-data.js`, which had NEVER worked (omitted required columns, stringified jsonb, swallowed its own insert error while printing success). And nearly pushed a false green — piped test output through `tail`, which cut off an '8 failed' header; caught only because the counts didn't reconcile against the declared total.

4 commits (0a3ef1d, c4b1e67, 5134dd6, 6e6afb2 pushed; 8b9bb97 runner-table polish committed but deliberately NOT pushed). Full suite 124 passed / 2 skips / 0 failed.

Session ended in planning: beta is 31 July and the #1 risk is RLS — three consecutive sessions found real gaps, all BY ACCIDENT, and /deploy-check's RLS check would have passed all three. Next session: build a behavioural RLS audit harness incl. cross-COACH isolation, which has never been tested.

## 2026-07-11 — CoachApp session 25 (part 3)

Jake used the runner in a real gym session and came back with 4 corrections — the most interesting being **remove the plate calculator**, a feature shipped only 8 days earlier because it was 'repeatedly requested' in the 2026-07-02 competitor research. It did not survive one real session. Same message reversed the pre-fill/'1-tap repeat' behaviour, another Hevy import: a pre-filled value is indistinguishable from one you actually entered, so a set could be ticked off without the weight ever being confirmed. **Lesson banked (les-039): the research said build it, the gym said remove it.** Committed as 8b9bb97 but deliberately NOT pushed, at his request.

Session then pivoted to planning. The most valuable finding was something nobody had ever asked: **a brand-new coach signs up to a completely empty app.** handle_new_user creates only the profiles row — 0 exercises, 0 templates, 0 programs. You cannot build a workout without exercises, and the only route is typing each one in by hand. Jake has never seen this; his account has 200+ exercises accumulated over months. The app is excellent once populated and close to unusable before. Highest beta risk found so far, with beta 20 days out.

Next session decided: **Security & beta gates** — a behavioural RLS audit harness including cross-COACH isolation, which has never been tested. Three consecutive sessions found real RLS gaps and all three were found by accident; /deploy-check's RLS check would have caught none of them.

---

**2026-07-12 — Session 26 — Security & beta gates; a live storage breach, caught.**

Built the behavioural RLS audit (rls-audit.spec.js) that last session set up as the priority, and ran /deploy-check end-to-end for the first time in the project's history. It paid for itself immediately: it found a LIVE cross-tenant storage leak. progress-photos was public=false — which every prior check treated as "secure" — yet had three storage.objects policies scoped by bucket_id alone, so any authenticated coach could read AND delete any client's progress photos. Reproduced live: a second coach (the first-ever created, for this audit) who owns nothing downloaded a real 1.79MB photo and deleted another. Fixed by dropping the three over-broad policies; proven red→green.

The audit also found Personal Bests had NEVER displayed for anyone — it embedded a table that doesn't exist, the error was swallowed, the page said "no personal bests" forever. And the pre-push multi-agent review caught two real runner bugs in last session's committed-but-unpushed polish before they shipped: a set-adder that still auto-filled last session's numbers (so you could log a set you never did), and a duplicated "RPE" label the de-dup fix had missed because the wizard held a verbatim copy of the helper. My own fix for the second then broke the runner entirely — caught by the full suite, not by me.

Three "a config-read passed a real leak" moments now: the RLS qual='true' grep, and today the storage public flag. The through-line, banked as les-040: config reads describe intent; only attempting the operation as the wrong tenant reveals behaviour. Both checks are now behavioural probes in CI. Also removed the progress-photos feature (Jake, "for now" — code out, data kept), wrote the ICO breach procedure (with today's leak as a worked non-notification), and pushed 5 commits green. pth-120 (the audit will find another gap) graded TRUE same-session. Beta is 31 July; the empty-new-coach blocker is still the top unbuilt risk.

---

**2026-07-12 — Session 26 cont. — 3 program bugs from real use, then the empty-app beta blocker SOLVED.**

Two workstreams in one continuation. (A) Three program-workflow bugs Jake hit while actually coaching: assigning a program showed stale data until refresh (fire-and-forget clone, now awaited); "Update all same-named sessions" was overwriting the ENTIRE target workout, not just the changed exercise (now a surgical by-name propagation); and editing a program's workout never reached the assigned calendar copy, forcing a re-assign (now auto-syncs — the coach's own solo copies silently, real clients behind a confirm). The multi-agent review earned its place again, catching two more real bugs in the first cut (a stale-change replay on delete, a real-client consent bypass) before push. (B) The empty-app beta blocker — the highest open beta risk — is SOLVED, built through the full superpowers brainstorm→spec→plan→implement→review flow: a brand-new coach's first login now seeds ~40 curated exercises + a sample workout + a sample program, gated by a profiles.starter_seeded flag, app-side so the list stays editable. The review's one finding (a partial-failure could strand a coach half-onboarded) was fixed by making the seed fully resumable, with a regression test. Along the way an onboarding test stranded 40 rows in the shared PT2 account and compounded to 80 — cleanup-after-asserts skipped on failure — banked as les-041 (self-heal at start + teardown in finally). 6 commits, all pushed, CI green, 133 passed. Beta 31 July; only a real new-coach signup remains to confirm onboarding live.
2026-07-18 — CoachApp: week-tabs redesign (Programs builder + client Workouts page) pushed 07febfa, CI green; multi-agent review restored a silently-dropped Save-to-Library + escaped a pre-existing DAY-header XSS; os-lint gained a stale-predictions check; hello-claude Step 8 removed.
2026-07-19 — CoachApp: Progress-overhaul CAPTURE layer built end-to-end on branch progress-overhaul (①data-model + ②b save-persistence + ②a builder metric_type picker + ②c adaptive fast table), UNPUSHED (18 commits ahead of master); ① migration + ②a backfill run live by Jake in Supabase; full suite 156/2/0. Remaining: ②d HR inputs, ③ display rebuild, ④ coach parity, then multi-agent-review + push. Model revised to 6 metric_types (AMRAP = per-set flag). Banked feedback_paste_sql_inline + les-044 (verify subagent actual state) + les-045 (concurrent-test contamination).
