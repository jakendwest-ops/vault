---
id: 2026-07-13-actually-root-caused-fixed-live-2026-07-30
status: fixed-awaiting-jake
priority: high
reported: 2026-07-13
status_detail: "fixed — awaiting Jake"
---

# ACTUALLY ROOT-CAUSED + FIXED + LIVE 2026-07-30

✅ **ACTUALLY ROOT-CAUSED + FIXED + LIVE 2026-07-30 — the 2026-07-29 fix was real but incomplete; Jake re-reported the identical repro the very next session with exact steps (programs > part time athlete > full body workout > edit workout > add exercise > enter any exercise and save).** The 2026-07-29 fix (`_afterTemplateExerciseSave`, below) was correctly built and DID close the propagation-chain failure mode it targeted — but it never ran, because `saveExerciseToTemplate` itself crashed one step earlier: it called `closeModal('add-to-template-modal')` (a real `document.getElementById(id)?.remove()`) and THEN re-read `document.getElementById('att-notes').value`/`att-superset` a second time to build `window._lastExerciseChange.row` — throwing `Cannot read properties of null (reading 'value')` against the now-deleted modal, aborting the function before it ever reached the new re-render call. The INSERT had already committed by that point, so the exercise really was added — it just never repainted without a manual refresh, exactly matching every one of the 4 reports of this symptom. Fixed by capturing `notes`/`supersetGroup` into local consts once, before `closeModal` runs, reused for both the insert payload and `_lastExerciseChange`. Red→green `tests/ledger-fixes-2026-07-30.spec.js` (A3-cont), driven through the REAL modal/DOM (not a mock) so the exact crash reproduces. **Needs your eyes — 4th report of this symptom, please re-run your exact repro steps above.** — (orig 2026-07-29 fix, real but insufficient) **Adding a new exercise to a program workout did NOT offer "update all same-named sessions."** Jake, live. The propagate-on-edit path shipped 2026-07-12 (`_lastExerciseChange` captures add/update/delete; `_checkClientPlanPropagation` fans out) — so this is a **regression in fresh code**, or the ADD verb never reached the capture. Investigate the add path specifically; update/delete may work while add silently doesn't. ⚠️ **RE-REPORTED 2026-07-22, unprompted, from the Programs page** — 9 days open, never investigated. Driven live 2026-07-23 (during that day's session) — **neither symptom reproduced**, no fix attempted from a guess. ⚠️ **RE-REPORTED A 3RD TIME, 2026-07-28**, mid-intervals-session, with a sharper repro path than either prior report: *Programs > select existing workout > add exercise > save exercise > the newly added exercise does not appear in the UI until the page is refreshed.* This may be the SAME underlying symptom described two different ways across three reports (a save→render gap on the add-exercise path) rather than the propagate-to-siblings gap the original 2026-07-13 title describes — worth re-reading Jake's exact words fresh rather than assuming continuity with the older framing. Deliberately queued behind the intervals build to avoid running two active investigations through the same files (`app-workouts.js`/`app-programs.js`) at once — next up. 15 days open across 3 reports, still not investigated with his actual repro steps in hand.
