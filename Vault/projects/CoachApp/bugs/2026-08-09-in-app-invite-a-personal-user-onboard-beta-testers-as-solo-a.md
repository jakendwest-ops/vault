---
id: 2026-08-09-in-app-invite-a-personal-user-onboard-beta-testers-as-solo-a
status: fixed-awaiting-jake
priority: high
reported: 2026-08-09
status_detail: "fixed — awaiting Jake to deploy the Edge Function + confirm live"
---

# in-app "Invite a personal user" (onboard beta testers as solo accounts, no terminal)

✅ **SHIPPED + LIVE, CONFIRMED 2026-08-09 (`1a5cb72`) — in-app "Invite a personal user" (onboard beta testers as solo accounts, no terminal).** Edge Function deployed by Jake via the Supabase Dashboard's browser editor ("Deploy a new function" → "Via Editor", no CLI). **Verified end to end, both halves**: (1) non-owner rejection — signed in as the E2E test PT account and called the deployed function directly, got a genuine server-side `403 {"error":"Not authorized"}`, confirming the security boundary is real and not just client-side UI; (2) the real success path — Jake sent a live invite through the Settings card, the accepting user's profile came back exactly as designed: `role:'solo', coach_id:null, user_id` self-referential, `starter_seeded:false` (seeds correctly on their first login, per the earlier-confirmed starter-content fix). Closes the original ask cleanly — every future beta-tester invite is now Settings → fill in name/email → Send, no terminal or key ever again. — (orig) **SHIPPED, AWAITING DEPLOY 2026-08-09 (`1a5cb72`) — in-app "Invite a personal user" (onboard beta testers as solo accounts, no terminal).** Jake needed to onboard a friend for beta testing as a standalone solo/personal account. Confirmed first: public self-signup was removed entirely 2026-07-24 (the Scott West incident), and no scripted/in-app path for creating a NEW solo account has existed since — the only account-creation path is a coach inviting an existing client into their own roster. First attempt was a local Node script using the Supabase service-role key (`SUPABASE_SERVICE_KEY=... node scripts/invite-solo-user.cjs email name`) — functionally correct but Jake pushed back hard: *"too convoluted, won't scale... even if we have to build the solution."* **Rebuilt as the durable version**: `supabase/functions/invite-solo-user/index.ts` (new Edge Function — the only place a service-role-level capability may safely live) + a Settings-page card gated to `jakendwest@gmail.com` (the same literal-email pattern as `sudoAsClient`/"View as", the only existing owner-gate convention in this codebase) + `inviteSoloUser()` mirroring the existing (untested but production-proven) `sendClientInvite` function's shape exactly. The Edge Function independently re-verifies the caller's identity server-side (`supabase.auth.getUser()` against the caller's own token, hard 403 unless `caller.email==='jakendwest@gmail.com'`) — the Settings-card gate is a UI convenience only, not the security boundary. On success: real Supabase invite email → same tested `#invite-form` acceptance screen every client invite already uses → `profiles.role='solo'` + a self-referential `clients` row (`user_id`=own account, `coach_id: null`) — never a coach-linked row at any point, unlike the client-invite flow. **Multi-agent review (3 angles) caught 5 real issues, all fixed same session**: (1) no guard against silently converting an ALREADY-EXISTING coach/client account into solo if Jake ever reused/mistyped an email — now refuses with a 409 and a clear message instead of overwriting; (2) a swallowed error on the `clients` existence-check; (3) the local script's final log line printed the invitee's name (PII) — trimmed to id/role/status only; (4) a stale button-revert `setTimeout` could stomp a second invite's in-progress "Sending…" label if sent within 3s of the first (not a double-submit, just a misleading visual state) — fixed with a tracked/cleared timer; (5) the "Name and email required" validation message never cleared on a later successful send. 6 feature tests including 2 new regression tests locking in fixes (4)/(5). Full suite 365 passed / 5 pre-existing `client-workout.spec.js` failures (unchanged baseline) / 2 skipped, no flakes. mobile-checked at 390×844. **NOT YET LIVE — needs Jake to deploy the Edge Function once, via the Supabase Dashboard (Edge Functions → Deploy a new function → name it `invite-solo-user` → paste `supabase/functions/invite-solo-user/index.ts`'s contents → Deploy) — browser-only, no CLI, since this repo has no Supabase CLI/config tracked and Jake doesn't have it installed.** Once deployed, every future beta-tester invite is just Settings → fill in name/email → Send, forever — this is what actually closes the "won't scale" complaint.
