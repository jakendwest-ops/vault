---
id: 2026-07-24-solo-is-now-a-genuine-independent-account-type-at-the-data-m
status: fixed-awaiting-jake
priority: high
reported: 2026-07-24
reported_detail: updated 2026-08-01
status_detail: "fixed — awaiting Jake (SQL run live same date; not yet pushed; not yet live-verified — see top of file)"
---

# Solo is now a genuine, independent account type at the data-model level

✅ **SHIPPED (LOCAL MERGE ONLY) 2026-08-01 (2nd save, `1ef09c9`) — Solo is now a genuine, independent account type at the data-model level.** See top of file for the full build writeup (spec → plan → 4-task SDD build → final review → merge). `profiles.role='solo'` is now a real, permanently-stored value for the `solo_only` account instead of a computed reassignment; the starter-content seeding bug this ledger's earlier 2026-08-01 entry found is fixed as part of the same work. Public signup for solo accounts remains explicitly out of scope (Jake's own call during brainstorming — separate future conversation). Extensive red→green test coverage (task-level + final-review fix-wave tests), but **not yet closed** per this ledger's own rule: needs Jake to (1) run the SQL migration, (2) push to origin, (3) confirm live on the real account. — (orig, now historical) **SLIPPED PAST BETA 2026-07-28 — DECISION NEEDED — Solo must become a genuine, independent account type.** Jake, 2026-07-24, live, after the Scott West signup incident (see Security): *"I want it to become its own genuine account type."* **Today's reality, confirmed by reading the code closely while fixing that incident:** solo is NOT a real account — it's a mode layered on top of a PT account. Same `auth.uid()`, `profiles.role` stays `'coach'` in the DB always, and a self-referential `clients` row (`user_id = own id, coach_id = NULL`) is what unlocks a "Personal" view via `window._masterAccount`/the view-switcher (`js/app-core.js` `loadUserInfo`, ~line 166). There is no way today to create an account that is genuinely *just* solo with no PT capability underneath — proven by the `solo_only` flag (below) having to exist as a workaround at all. Originally banked 2026-07-24 with an explicit target of "before beta testing begins (31 July 2026)". **2026-07-28: deprioritized again, this time explicitly past that date.** Jake asked to build Solo right after that session's second `/save`; a live bug report ("no imperial/metric toggle, no intervals visible") interrupted almost immediately, which surfaced two new, unrelated feature asks (the units toggle — resolved and shipped 2026-07-25 — and an intervals redesign, Jake's own Tabata-style reference screenshots). Given the beta timeline, Jake was asked to choose between resuming Solo or building the intervals redesign; he chose **intervals now**, explicitly accepting Solo would likely slip past beta. The intervals redesign then ran long (34 commits: the feature itself, a whole-branch security review that surfaced and closed 5 rounds of a recurring client→coach stored-XSS pattern touching `client_check_ins`/`clients`/`goals`/`events` — see LOG 2026-07-28), so nothing further happened on Solo this session either. Data-model scoping session happened 2026-08-01 (brainstorm → approved spec → plan); build completed same date, 2nd save — see the ✅ entry above this one.
